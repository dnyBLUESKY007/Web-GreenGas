# GREENGAS Issue 22 Release Candidate

- **日期：** 2026-07-27
- **分支：** `main`
- **唯一应用候选输入：** `9742d1ba7998c37e0239f63bedb80f6822320d10`
- **候选状态：** 验收通过，可部署
- **部署状态：** 本记录未执行生产部署

## 固定输入

本候选固定在已推送至 `origin/main` 的 `9742d1ba7998c37e0239f63bedb80f6822320d10`。Issue #22 仅新增候选证据和状态记录，不修改 `frontend/`，因此后续文档提交不改变应用构建输入。

| 输入 | 记录值 |
|---|---|
| Node.js | `v24.18.0` |
| npm | `11.16.0` |
| `package-lock.json` SHA-256 | `a987614eddd7000b5701f6f40c014c562f45bc739e7efe8b5dd673cf8ec062a8` |
| 根路径候选文件数 | 47 |
| 根路径候选 SHA-256 | `2062378ee7358e80d5de16de332b23b42b1d4d6516811f588908d3ad291f9902` |
| `/rc/` 子路径候选文件数 | 47 |
| `/rc/` 子路径候选 SHA-256 | `537b35b3206032b66f3d58c60dab0af5ab2d02a8cc8924595f66f0d950399acd` |

候选摘要按路径排序，对每个文件依次写入“相对路径、NUL、字节数、NUL、文件内容”，再计算整体 SHA-256。根路径和 `/rc/` 构建均连续执行两次，文件数及摘要分别保持一致。

## 验证结果

| 检查 | 结果 |
|---|---|
| `npm ci` | 通过；按锁文件安装 25 个包 |
| `npm run typecheck` | 通过 |
| `npm run test` | 23/23 通过 |
| 根路径生产构建 | 通过；两次均为 47 个文件及 `206237…f9902` |
| `VITE_BASE=/rc/` 子路径构建 | 通过；两次均为 47 个文件及 `537b35…99acd` |
| 多视口浏览器检查 | Chrome 150 通过 320、390、768、1024、1440px |
| 关键页面 | 首页、案例列表、布里斯班案例详情共 15 个视口/路由组合均无页面级横向溢出 |
| 首页案例区 | 4 张案例卡、1 个复用地图、3 个已核实案例点及地图列表/详情链接均有效 |
| 案例中心 | 4 张案例卡、3 个已核实案例点，地图列表链接均可解析 |
| 三语行为 | `en`、`zh`、`ru` 的 `document.lang`、本地存储和激活状态同步；标题保持“格灵空调” |
| 新增 OSS 图片 | 23/23 返回 HTTP 200 和 `image/webp` |

## 已知非阻塞问题

- 浩达工具案例未提供项目地点，因此按证据边界保留为无地图点的已核实案例；发布总结需要继续向甲方索取地点。
- 地图视觉重绘不属于本候选，由独立 GitHub #24 跟踪。
- `npm audit` 仍报告 `immutable` 和 `postcss` 两项可修复的高危传递性开发依赖告警。它们用于本地静态构建，不进入浏览器生产产物；依赖升级未混入本次内容发布。
- EmailJS、最终产品目录和技术资料仍按既有独立范围保持未配置或明确占位状态。

## 发布边界

本记录证明 `9742d1b` 应用候选满足发布前验收，但本身不修改生产环境。生产部署、部署前备份、线上路由/资源核验和失败回滚属于 GitHub #23。
